<TelaApp>:
    # rows: 2 # número de linhas
    row_default_height: 50 # tamanho das linhas do widgets
    row_force_default: True # Forçando as linhas dos widgets à ter o tamanho 50

    cols: 4 # número de colunas
    col_default_width: 80 # tamanho das colunas do widgets
    col_force_default: True # # Forçando as colunas dos widgets à ter o tamanho 10
    Button:
        text: 'A'
    Button:
        text: 'B'
    Button:
        text: 'C'
    Button:
        text: 'D'
    Button:
        text: 'E'
    Button:
        text: 'F'